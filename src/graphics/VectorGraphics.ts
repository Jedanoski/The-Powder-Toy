class VectorGraphics {
  constructor() {
    console.log("VectorGraphics initialized");
  }

  drawCircle(x: number, y: number, radius: number) {
    console.log(`Drawing circle at (${x}, ${y}) with radius ${radius}`);
  }
}

export default VectorGraphics;